version: '3.8'

services:
  neo4j:
    image: neo4j:5
    container_name: my_neo4j
    ports:
      - "7687:7687"     # Bolt
      - "7474:7474"     # HTTP browser
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
